{
  "name": "emobu",
  "private": true,
  "version": "0.0.1",
  "description": "mobu timer",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "serve dist -l 8938 -n",
    "test": "elm-test",
    "lint": "elm-review && npm run eslint && npm run stylelint:check",
    "check": "typos && npm run format:check && npm run typecheck && npm test && npm run lint",
    "preview": "vite preview",
    "format:check": "dprint check && npx elm-format --validate src",
    "format:fix": "dprint fmt && npx elm-format src",
    "eslint": "eslint .",
    "eslint:fix": "eslint --fix .",
    "stylelint:check": "npx stylelint '{src,public}/**/*.css'",
    "stylelint:fix": "npx stylelint '{src,public}/**/*.css' --fix",
    "typecheck": "tsc",
    "deps": "nix --version && dprint --version && typos --version && node --version && npm --version && elm --version"
  },
  "author": "Kenichi Kamiya <kachick1@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node20": "^20.1.0",
    "@tsconfig/strictest": "^2.0.1",
    "@typescript-eslint/eslint-plugin": "^6.2.0",
    "@typescript-eslint/parser": "^6.2.0",
    "elm-format": "^0.8.7",
    "elm-json": "^0.2.13",
    "elm-review": "^2.10.2",
    "elm-test": "^0.19.1-revision12",
    "eslint": "^8.45.0",
    "stylelint": "^15.10.2",
    "stylelint-config-recommended": "^13.0.0",
    "typescript": "^5.1.6",
    "vite": "^4.4.6",
    "vite-plugin-elm": "^2.8.0"
  },
  "dependencies": {
    "elm": "^0.19.1-5"
  }
}
